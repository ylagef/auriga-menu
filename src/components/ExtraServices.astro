---
import type { EXTRA_SERVICES } from '../types'

export interface Props {
  extraServices: EXTRA_SERVICES[]
}

const { extraServices } = Astro.props
---

<div class="flex flex-col gap-1">
  {
    extraServices.map((extraService) => {
      return <small class="text-center">{extraService}</small>
    })
  }
</div>
