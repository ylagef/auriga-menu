---
import Product from '@components/app/Product.astro'
import { ProductSI } from 'src/types'

export interface Props {
  products: ProductSI[]
}

const { products } = Astro.props
---

<div class="flex flex-col gap-6">
  {products.sort((a, z) => a.order - z.order).map((product) => <Product product={product} />)}
</div>
